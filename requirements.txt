# WeasyPrint is the only runtime dependency. It converts the assembled
# HTML to PDF. The assembler runs without it (HTML still produced), but
# the PDF step is skipped with a notice.
weasyprint>=60.0
